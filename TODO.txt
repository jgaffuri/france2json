filter bn on client side - remove the ones with stroke:none
compress file

compress more
use -q
"-q: maximum number of differentiable points along either dimension". -> take the map size in pixels? *10 ?
"When you run topojson on the command line, it will output the quantization precision in meters along x and y"
see pre-post-quantization parameters:
"Pre-quantization occurs before the topology is constructed, and is controlled by --pre-quantization or --q0. Pre-quantization may produce simpler topologies from messy inputs, but should be disabled to preserve the input topology exactly."
"Post-quantization occurs after the topology is created which, and is solely for producing smaller files (e.g., for web delivery). Post-quantization is controlled by --post-quantization or --q1"
"When using Cartesian coordinates, the meaning of the simplification threshold changes to be in projected coordinates. In conjunction with the --width and --height arguments (described below), you can specify a simplification threshold in pixels, which is convenient!" -> take 1 pixel? or 0.1?
'compress' borders? check first with mouse over
filter points (use --filter? Applies for lines?)

use bbox?
"To include a bbox property in the generated topology that represents the bounding box of all contained features, use the --bbox argument. The bounding box array follows the same format as GeoJSON: [x0, y0, x1, y1]."

semantic zoom. develop own, based on svg-transform. use line style redimension with scale factor
http://bl.ocks.org/mbostock/2a39a768b1d4bc00a09650edef75ad39




move corsica
add DOMs, etc

produce json for departements, zones electorales

ogrinfo -so -al myfile.shp
communes
INSEE_COM: String (5.0)
NOM_COM: String (50.0)
STATUT: String (25.0)
X_CHF_LIEU: Integer (7.0)
Y_CHF_LIEU: Integer (7.0)

limite communes
NATURE: String (200.0)


CASE
WHEN NATURE='Frontire internationale' THEN 'i'
WHEN NATURE='Limite ctire' THEN 'o'
WHEN NATURE="Limite d''arrondissement" THEN 'a'
WHEN NATURE='Limite de commune' THEN 'c'
WHEN NATURE='Limite de dpartement' THEN 'd'
WHEN NATURE='Limite de rgion' THEN 'r'
END

CASE
WHEN STATUT="Capitale d''tat" THEN 'c'
WHEN STATUT='Commune simple' THEN 's'
WHEN STATUT='Prfecture de dpartement' THEN 'd'
WHEN STATUT='Prfecture de rgion' THEN 'r'
WHEN STATUT='Sous-prfecture' THEN 'o'
END
